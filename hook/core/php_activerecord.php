<?php
return;
/*
bu::lib('php-activerecord/ActiveRecord');
ActiveRecord\Config::initialize(function($cfg) {
     $dbs = bu::config('db');
     $connections = array();
     foreach($dbs as $k=>$v)
         $connections[$k] = sprintf('%s://%s:%s@%s/%s',
                                    $v['driver'],
                                    $v['user'],
                                    $v['password'],
                                    $v['host'],
                                    $v['database']);
     $cfg->set_connections($connections);
     $cfg->set_default_connection('default');
});

*/
